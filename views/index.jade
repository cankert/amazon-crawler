extends layout

block content

  div.container
    h1 Amazon Crawler
    div.form-group.col-md-12
        h3 Add Website
        input.form-control(type="text", placeholder="Website URL", id="urltosubmit")
        input.form-control(type="submit", value="Submit", id="submiturl")

        h3 Update Entries
        input.form-control(type="submit" value="Update all" id="updateall")
    table#producttable.table
        thead
            th ASIN
            th Name
            th Image
            th Aktueller Preis
        tbody
